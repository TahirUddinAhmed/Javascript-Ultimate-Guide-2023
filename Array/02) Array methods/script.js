// array
const arr = [2, 34, 100, 54, 234];

// push
arr.push(97);
// pop
arr.pop(); // remove elements from the end of the array
// unshift
arr.unshift(12);  // add element in the beginning of the array
// shift
arr.shift(); // remove element from the beginning of the array


console.log(arr);

